import assert from 'assert';
import knockoutDot from '../lib';

describe('knockout-dot', function () {
  it('should have unit test!', function () {
    assert(knockoutDot, 'we expected this package author to add actual unit tests.');
  });
});
